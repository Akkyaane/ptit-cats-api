'use strict';

/**
 * animal-requirement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::animal-requirement.animal-requirement');
