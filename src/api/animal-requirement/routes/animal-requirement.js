'use strict';

/**
 * animal-requirement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::animal-requirement.animal-requirement');
