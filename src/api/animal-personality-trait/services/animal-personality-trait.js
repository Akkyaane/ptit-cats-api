'use strict';

/**
 * animal-personality-trait service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::animal-personality-trait.animal-personality-trait');
