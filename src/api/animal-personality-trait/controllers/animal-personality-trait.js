'use strict';

/**
 * animal-personality-trait controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::animal-personality-trait.animal-personality-trait');
