'use strict';

/**
 * animal-personality-trait router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::animal-personality-trait.animal-personality-trait');
